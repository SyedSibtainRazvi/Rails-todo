<!DOCTYPE html>
<html>
  <head>
    <title>TodoApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="flex flex-col h-screen">
    <header class="flex items-center p-4 text-white bg-gray-800">
      <h1  class="text-2xl"><%= link_to "Rails Todo App", root_path %></h1>
      <ul class="ml-16">
        <li> <%= link_to "New Todo", new_todo_path %></li>
      </ul>
    </header>
    <main class="flex-1 p-4 overflow-auto">
      <div class="flash notice">
        <%= flash[:notice] %>
      </div>
      <%= yield %>
    </main>
    <footer class="p-4 text-white bg-gray-800">
      <p>&copy; 2024 TodoApp</p>
    </footer>
  </body>
</html>
